# ResNet-56 (n=9 blocks per stage) — same optimization recipe as resnet20.yaml
model_depth: 56
epochs: 200
batch_size: 128
lr: 0.1
momentum: 0.9
weight_decay: 0.0005
nesterov: true
label_smoothing: 0.1
warmup_epochs: 5
warmup_start_factor: 0.1
use_amp: true
seed: 42
num_workers: 4
